#ifndef UPPER_BOUND_LOCATION_HPP
#define UPPER_BOUND_LOCATION_HPP
#include <string>
#include "models.hpp"
// upper_bound on primaryKey
int upperBoundByLocation(const Candidate arr[], int n, const std::string& key);
#endif
