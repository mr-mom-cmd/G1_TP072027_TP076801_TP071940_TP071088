#ifndef LOWER_BOUND_LOCATION_HPP
#define LOWER_BOUND_LOCATION_HPP
#include <string>
#include "models.hpp"
// lower_bound on primaryKey
int lowerBoundByLocation(const Candidate arr[], int n, const std::string& key);
#endif
